import React, { useEffect } from "react";

const VideoBackground = ({ movieId }) => {
  const getBackgroundMovieVideo = async () => {};

  useEffect(() => {
    getBackgroundMovieVideo();
  }, []);
  return <div>VideoBackground</div>;
};

export default VideoBackground;
